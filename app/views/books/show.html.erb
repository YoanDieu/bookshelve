

<main>


  <ul class="bookcontainer">
  <ul class="bookalone">
    <%= form_tag "/books/#{@book.id}", method: "delete" do %>
      <input type="submit" value="x"/>
    <% end %>
    <div class="container">
      <%= form_tag "/books/#{@book.id}", method: "patch" do %>
        <p>Titre:</p>
        <input type="text" name="title" value="<%= @book.title %>" placeholder="<%= @book.title %>" />
        <aside class="error"> <%= @book.errors[:title].first %></aside>
        <p>Sous-titre:</p>
        <input type="text" name="sub_title" value="<%= @book.sub_title %>" placeholder="<%= @book.sub_title %>" />
        <p>Auteur:</p>
        <input type="text" name="author" value="<%= @book.author %>"placeholder="<%= @book.author %>"/>
        <aside class="error"> <%= @book.errors[:author].first %></aside>
        <select name="category_id" placehorder="Catégories" value="<%= @book.category.name %>">
          <% @categories.each do |category| %>
            <option value="<%= category.id %>"> <%= category.name %> </option>
          <%end%>
        </select>
        <p> Lu:<input type="checkbox" name="read" value="<%= @book.read %>"/></p>
        <input type="submit" value="Modifier le livre"/>
      <% end %>
    </ul>
  </ul>


  <div class="pagelink">
  <a href='/books'> < Retour à la bibliothèque </a>
  </div>

</main>
